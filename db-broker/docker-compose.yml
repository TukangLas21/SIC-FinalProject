services:
  postgres:
    image: postgres:15-alpine
    container_name: sic_postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: sic_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  hivemq:
    image: hivemq/hivemq-ce:latest
    container_name: sic_hivemq
    ports:
      - "1883:1883" # Standard MQTT Port (TCP)
      - "8080:8080" # HiveMQ Web UI 
    volumes:
      - ./hivemq/conf:/opt/hivemq/conf
      - ./hivemq/data:/opt/hivemq/data
      - ./hivemq/log:/opt/hivemq/log

volumes:
  postgres_data: